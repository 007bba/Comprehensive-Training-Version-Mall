技术细节被问到怎么说，面试的时候                                                                                                
                                                                                                                                  
● 面试里被问到技术细节时，不要只背“用了 Vue、Django、JWT”，要按 为什么用、怎么实现、代码里怎么体现、有什么不足和优化              
  这四层回答。你可以用下面这些话术。                                                        

  1. 如果问：你这个项目整体架构是什么？                                                                                              
  可以这样说：                                                                                                                    
  
  这个项目是前后端分离架构。前端是 myshop-vue，使用 Vue 2、Vue Router、Vuex 和
  Axios，主要负责页面展示、路由跳转、用户交互和接口调用。后端是 myshop-api2，使用 Django REST Framework 和
  MySQL，主要负责商品、分类、用户、购物车、订单、地址等业务接口。

  前端页面通过 Axios 请求后端 REST API。用户登录后，后端返回 JWT token，前端保存
  token，后续访问购物车、订单、地址等需要登录的接口时，会通过请求拦截器自动把 token 放到请求头里。

  可以补一句：

  所以这个项目的主线就是：前端负责展示和交互，后端负责数据和业务逻辑，两边通过 REST API 通信。

  2. 如果问：为什么要前后端分离？

  可以这样说：

  前后端分离可以让前端和后端职责更清楚。前端专注页面展示和用户体验，后端专注接口、数据库和业务规则。比如商品列表页前端只负责把分类
  、价格、排序、页码这些参数传给 /goods/ 接口，后端负责根据参数查询数据库并返回商品数据。

  这样做的好处是后端接口可以复用，不仅 Vue 前端能调用，将来如果有移动端、小程序或者管理后台，也可以继续使用同一套接口。

  3. 如果问：Vue Router 在项目里怎么用？

  可以这样说：

  Vue Router 负责管理前端页面跳转。项目里的路由配置在 src/router/index.js，比如 /index 对应首页，/list/:id
  对应商品列表，/detail/:id 对应商品详情，/cart 对应购物车，/checkout 对应结算页。

  其中 :id 是动态路由参数。比如进入 /detail/5，商品详情页就可以通过 this.$route.params.id 拿到商品 id，然后调用后端 /goods/5/     
  接口获取商品详情。

  还可以补一句：

  项目里也用了路由的 meta.title，在全局路由守卫里根据不同页面修改浏览器标题。

  4. 如果问：Axios 是怎么封装的？

  可以这样说：

  项目把 Axios 封装在 src/utils/request.js 里，统一配置了后端基础地址 http://localhost:8000/ 和请求超时时间。然后在 src/api       
  目录下按照业务模块封装接口，比如商品接口在 goods.js，购物车和订单接口在 order.js，用户接口在 users.js。

  这样页面组件不直接写 Axios 请求，而是调用封装好的业务方法。比如商品详情页调用 getGoodsByID(id)，购物车页调用 getCart() 和       
  updateCart()。

  如果面试官追问拦截器，可以说：

  请求拦截器主要负责给需要登录的接口自动添加 JWT token。响应拦截器主要处理认证失败，比如后端返回 401 时提示用户先登录。

  5. 如果问：JWT 登录认证怎么实现？

  可以这样说：

  用户登录时，前端把用户名和密码提交到后端 /login/ 接口。后端使用 djangorestframework-jwt 生成
  token，并且项目自定义了认证后端，支持用户名或手机号登录。

  登录成功后，前端把 token 保存到 localStorage，同时把用户信息保存到
  Vuex。后续请求购物车、订单、地址、个人资料这些需要登录的接口时，Axios 请求拦截器会自动读取本地 token，并添加请求头：

  Authorization: JWT <token>

  后端通过 JWT 解析出当前用户，然后接口中就可以使用 request.user 获取当前登录用户。

  如果要显得更懂，可以补一句：

  这样做的好处是后端不用依赖传统 session，前后端分离项目中更方便；但 token 存在 localStorage 里也要注意 XSS
  风险，真实项目中可以结合 HttpOnly Cookie 或更严格的前端安全策略。

  6. 如果问：Vuex 在项目中解决了什么问题？

  可以这样说：

  Vuex 用来管理多个页面共享的数据。这个项目里主要保存登录用户信息和购物车列表。

  比如登录成功后，用户信息会保存到 Vuex，头部组件就可以显示用户名。购物车变化后，Vuex 会重新请求购物车数据，并通过 getter
  计算购物车总数量和总价格。这样头部购物车、购物车页面、结算页面都能共享同一份购物车状态，不需要每个组件自己重复请求和计算。      

  可以补一句：

  Vuex 的优势是让跨组件共享状态更集中，缺点是如果状态设计不清楚，也容易让数据流变复杂。

  7. 如果问：商品列表的筛选、排序、分页怎么做？

  可以这样说：

  前端商品列表页是 /list/:id，通过路由参数拿到分类 id。页面会把分类 id、最低价格、最高价格、排序字段和页码作为参数传给 /goods/    
  接口。

  后端 GoodsView 使用 DRF 的 ViewSet，配合 django-filter 实现过滤。过滤器支持按分类、最低价、最高价和名称过滤。排序通过 DRF 的    
  OrderingFilter 实现，支持按销量 amount 和价格 price 排序。分页使用自定义分页类，每页返回固定数量商品，并返回总数量
  count，前端根据总数量计算总页数。

  可以补一句：

  所以筛选、排序、分页主要由后端完成，前端只是收集查询条件并展示结果。

  8. 如果问：商品分类怎么设计？

  可以这样说：

  商品分类表 GoodsCategory 使用了自关联设计，也就是 parent 字段指向自己。这样可以实现一级分类和二级分类。顶级分类的 parent        
  为空，子分类的 parent 指向父分类。

  前端头部菜单和商品列表左侧分类都依赖这个分类接口。后端序列化分类时，会把子分类一起返回给前端，所以前端可以渲染“一级分类下面有多 
  个子分类”的菜单结构。

  9. 如果问：商品详情怎么实现？

  可以这样说：

  商品详情页路由是 /detail/:id，页面通过 this.$route.params.id 获取商品 id，然后调用 GET /goods/<id>/ 获取商品详情。

  后端商品详情接口在返回商品数据前，还会把商品的点击数 click_num 加
  1。前端拿到商品名称、价格、库存、主图、详情描述后展示出来，用户可以选择购买数量并加入购物车。

  10. 如果问：购物车怎么实现？

  可以这样说：

  购物车表 Cart 关联用户和商品，并保存商品数量 goods_num。用户点击加入购物车时，前端提交商品 id 和数量到 /cart/ 接口。

  后端新增购物车时会先查询当前用户是否已经添加过这个商品。如果已经有这条记录，就在原来的数量上累加；如果没有，就新增购物车记录。这
  样可以避免同一个用户对同一个商品产生多条购物车记录。

  购物车列表接口只返回当前登录用户自己的购物车数据，这一点通过 request.user
  过滤实现。前端拿到购物车列表后，可以计算小计、总价和总数量，并同步更新 Vuex。

  11. 如果问：为什么购物车接口只返回当前用户的数据？

  可以这样说：

  购物车是用户私有数据，所以后端不能返回所有购物车记录。项目在购物车视图里通过 Cart.objects.filter(user=self.request.user)        
  过滤，只查询当前登录用户的购物车。

  这样可以保证不同用户之间购物车数据隔离，也避免用户看到或修改别人的购物车。

  12. 如果问：购物车数量修改怎么做？

  可以这样说：

  前端购物车页点击加号或减号时，会调用 PATCH /cart/<goods_id>/ 接口，把新的商品数量传给后端。后端根据商品 id
  找到对应购物车记录并修改 goods_num。

  修改成功后，前端重新请求购物车列表，并调用 Vuex 的 saveCart 更新全局购物车状态，这样页面和头部购物车数量都会刷新。

  13. 如果问：订单提交怎么实现？

  这是重点，可以这样说：

  订单提交是项目后端最核心的业务。用户在结算页点击提交订单后，前端把联系人、手机号、地址、支付方式和备注提交到 /order/ 接口。     

  后端首先生成订单号，然后创建订单主表
  Order。接着查询当前用户购物车中的所有商品，对每个商品校验库存是否足够。如果库存足够，就扣减库存、增加销量，并创建订单明细       
  OrderGoods。订单明细里会保存商品、购买数量和下单时的价格。最后后端计算订单总数量和总金额，更新订单主表，删除当前用户购物车，并返
  回订单生成成功。

  如果想说得更技术一点：

  这个过程用了事务，保证订单主表、订单明细、库存和购物车状态要么一起成功，要么一起回滚，避免出现订单创建一半失败的数据不一致问题。

  14. 如果问：为什么订单要用事务？

  可以这样说：

  订单提交涉及多个数据库操作，比如创建订单主表、创建订单明细、扣减库存、增加销量、清空购物车。如果其中任何一步失败，都可能造成数据
  不一致。

  比如订单已经创建了，但是库存没扣，或者库存扣了但是订单明细没生成，这些都是错误状态。所以后端使用事务，把这些操作作为一个整体处理
  。成功就一起提交，失败就一起回滚。

  15. 如果问：为什么订单明细要保存价格？

  可以这样说：

  订单明细中保存下单时的商品价格，是为了保留价格快照。因为商品价格以后可能会变化，如果订单只关联商品表，不保存下单时价格，那么以后
  查看历史订单时价格可能会变成最新价格，导致订单金额不准确。

  所以订单明细应该保存购买数量和下单时价格，这样历史订单不会受商品价格变动影响。

  16. 如果问：库存是怎么处理的？

  可以这样说：

  项目在订单提交时会读取购物车商品，判断购买数量是否超过库存。如果库存不足，就返回错误并回滚事务。如果库存足够，就扣减商品库存，并
  增加商品销量。

  后端还使用了 select_for_update() 对商品记录加锁，目的是在并发下避免多个用户同时购买同一件商品时造成超卖。

  然后可以主动说不足：

  不过当前项目里购物车加入和订单提交两个阶段都有库存扣减逻辑，真实项目中更推荐只在提交订单时扣减库存，购物车只保存购买意向，这样更
  清晰，也避免重复扣库存。

  17. 如果问：后端的模型有哪些？

  可以这样说：

  后端主要模型有这些：

  用户模型 MyUser，继承 Django 的 AbstractUser，扩展手机号、真实姓名、性别、头像、会员等级等字段。

  商品分类模型 GoodsCategory，支持父子分类。

  商品模型 Goods，保存商品名称、分类、价格、库存、销量、主图和详情。

  轮播模型 Slide，用于首页轮播图。

  购物车模型 Cart，关联用户和商品，保存商品数量。

  订单模型 Order，保存订单号、总金额、联系人、地址、状态和用户。

  订单明细模型 OrderGoods，保存订单中的具体商品、数量和价格快照。

  地址模型 Address，保存用户收货地址。

  18. 如果问：后端 Serializer 的作用是什么？

  可以这样说：

  Serializer 有两个作用：第一，把数据库模型对象转换成前端能用的 JSON；第二，对前端提交的数据进行校验和保存。

  比如用户注册序列化器会校验用户名是否重复、手机号是否合法，并在创建用户时调用 set_password() 加密密码。购物车序列化器会重写
  create()，如果当前用户已经添加过同一个商品，就累加数量，而不是重复创建记录。

  19. 如果问：ViewSet 的作用是什么？

  可以这样说：

  ViewSet 是 DRF 提供的一种把增删改查接口组织在一起的方式。比如购物车的列表、新增、修改、删除都可以写在 CartViewset 里，然后通过  
  Router 自动生成对应 REST API。

  这样比每个接口都单独写一个视图函数更规范，也减少重复代码。

  20. 如果问：后端权限怎么控制？

  可以这样说：

  项目主要通过 JWT 识别当前用户。购物车、订单、地址等接口要求用户登录，后端通过认证类解析请求头里的 token，得到 request.user。    

  然后接口查询数据时都会按当前用户过滤，比如购物车只查
  user=request.user，地址只查当前用户的地址，订单只查当前用户的订单。这样可以保证用户只能访问自己的数据。

  21. 如果问：你觉得这个项目有什么不足？

  可以这样说：

  我觉得主要有几个可以优化的地方。

  第一，前端有一些备份文件和重复接口封装，可以清理目录结构，把接口按业务模块统一管理。

  第二，用户资料接口前端写成 /users/1/，但后端实际返回当前登录用户，这个语义不够清楚，可以改成 /users/me/。

  第三，购物车和订单阶段都存在库存扣减逻辑，真实项目中更建议只在订单提交时扣库存，购物车只保存购买意向。

  第四，地址默认值更新时没有按当前用户过滤，可能影响其他用户，应该加上 user=request.user 的条件。

  第五，数据库密码等配置写在配置文件里，正式项目应该用环境变量管理。

  这样回答显得你很懂，不是只会说优点。

  22. 如果问：你学到了什么？

  可以这样说：

  我主要学到了三个方面。

  第一，前后端分离项目的完整开发流程，前端通过路由组织页面，通过 Axios 调接口，通过 Vuex
  管理共享状态；后端通过模型、序列化器、视图集和路由提供 REST API。

  第二，商城项目的核心业务流程，包括商品展示、购物车、订单生成、库存处理和用户认证。

  第三，订单类业务要注意数据一致性，不能只考虑插入订单，还要考虑库存、订单明细、价格快照、购物车清空和事务回滚。

  23. 面试官连续追问时的万能回答结构

  你可以按这个结构回答任何技术细节：

  “这个功能的入口在前端哪个页面，前端调用哪个 API，后端哪个接口处理，后端操作哪些模型，最后返回什么结果。”

  比如购物车：

  商品详情页点击加入购物车，前端调用 addCart() 请求 /cart/，后端 CartViewset 处理请求，操作 Cart
  模型，如果已有商品就累加数量，没有就新增，最后返回购物车数据，前端再刷新 Vuex 中的购物车状态。

  这个结构非常稳。

  24. 最后给你一段可直接背的技术细节总述

  这个项目技术上是典型的前后端分离商城。前端用 Vue Router 管理页面，用 Axios 封装接口请求，用 Vuex 保存用户和购物车状态；后端用   
  Django REST Framework 的 Model、Serializer、ViewSet 和 Router 构建 REST API。用户登录后后端返回 JWT，前端保存 token，并通过     
  Axios 拦截器在需要登录的接口上自动携带
  token。商品模块支持分类、详情、筛选、排序和分页；购物车模块按当前用户保存商品和数量，并处理重复商品累加；订单模块从当前用户购物 
  车生成订单主表和订单明细，使用事务保证库存、销量、订单和购物车状态一致。这个项目的核心技术点就是前后端接口联动、JWT 认证、Vuex  
  状态共享、DRF 接口开发和订单事务处理。